- repo: local
  hooks:
    - id: SwiftLint
      name: SwiftLint
      entry: bash -c 'PATH=/usr/local/bin:$PATH brew bundle exec swiftlint -- --no-cache --strict'
      language: system
      pass_filenames: false

